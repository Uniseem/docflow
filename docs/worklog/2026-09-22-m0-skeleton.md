# 2026-09-22 M0 工程骨架

## 会话 1（Cursor Grok 4.6，16:14）

### 目标

执行 `docs/plan/09-milestones.md` 的 M0（M0-1 到 M0-7）。M0-8 需要推送后看 CI，本会话不推远程。

### 做了什么

- 按 02 章写入 `package.json`（`4.0.0`）、`.npmrc`，`npm install` 生成 `package-lock.json`。
- 配置：`electron.vite.config.ts`、`tsconfig*.json`、`eslint.config.js`、Prettier、Vitest、Playwright、`electron-builder.yml`。
- 最小应用：主进程单实例 + `docflow` 协议占位 + 窗口；preload 暴露空的 `window.docflow`；渲染进程 HeroUI `Button`、标题栏拖动区、深浅色切换。
- 脚本：`verify-fonts.mjs`、`make-icons.mjs`、`third-party-notices.mjs`（`prebuild` 需要它，否则 extraResources 缺文件）。
- 从 `old:apps/shared/AppIcon-1024.png` 取出 `build/icon.png`，生成 icns/ico。
- `npx heroui-cli@latest agents-md --react --output .heroui-docs/AGENTS.md`（目录已被 gitignore）。
- E2E：`tests/e2e/window-title.spec.ts` 启动 `--dir` 产物，断言窗口标题 `DocFlow`。
- 规划同步：02 章依赖表、`package.json` 全文、eslint/tsconfig/`dist` 脚本。

规划与现实不符，选择如下：

- `@vitejs/plugin-react` 规划写 `^6.1.1`，6.x peer 是 Vite 8；electron-vite 5 只到 Vite 7。锁 `@vitejs/plugin-react@^5.2.0`（仍支持 Vite 7）。
- eslint 配置 `import js from '@eslint/js'`，eslint 10 不把它当直接依赖装上。补 `@eslint/js@^10`。
- `dist` / `dist:dir` 改为 `npm run build && electron-builder …`，让 `prebuild` 生成 `THIRD_PARTY_NOTICES.md`。
- type-checked ESLint 对 `.mjs` / `eslint.config.js` 关掉；`tsconfig.node.json` 增加 `vitest.config.ts`、`playwright.config.ts`、`allowJs`。

### 怎么验证的

- `npm run check`：通过（字体 SHA-256、typecheck、eslint、prettier、vitest 占位 1 条）。
- `npm run dist:dir`：产出 `release/mac-arm64/DocFlow.app`，可启动。
- `npm run test:e2e`：1 passed（窗口标题 `DocFlow`）。
- Playwright 截图：浅色/深色都能看到「开始翻译」按钮和主题切换。
- `npm run dev`：electron-vite 把 `out/main/index.mjs` 与 `out/main/workers/{analyze,compose}.mjs` 都打出来；preload 为 `out/preload/index.cjs`。多入口 + ESM 可用。首次 `dev` 因 `node_modules/electron/dist` 未下载报 `Electron uninstall`，`require('electron')` 触发下载后正常。

### 没做成 / 坑

- M0-8：未推送，CI 尚未跑。本地 E2E 已绿。
- electron-vite 配置里 `__dirname` 可用（Vite 注入）；主进程运行时要用 `import.meta.url`。
- 本机第一次 `npm install` 没有拉到 Electron 二进制（可能被沙箱拦住 postinstall）；CI 的 `npm ci` 一般会跑 install 脚本。若 CI 也出现 `Electron uninstall`，在 `package` job 加一步 `node -e "require('electron')"`。

### 下一步

- 推 `main` 看 CI `check` + `package`（Windows / macOS）是否变绿，勾选 M0-8。
- 然后开始 M1-1：`src/shared/` 的 types/presets/constants/errors/text + 单测。

### 提交

- `feat: 搭好 4.0 工程骨架与空窗口`

## 会话 2（Cursor Grok 4.6，16:26）

### 目标

`/goal` 按规划做到 4.0 可发布。本段：修好 M0-8 的 CI 工作流，并完成 M1-1 共享层。

### 做了什么

- 推送 `1971095` 后 CI/Release 都在 0 秒失败、没有任何 job：job 级 `if: hashFiles('package.json')` 在 Actions 里非法。删掉该条件（`package.json` 已存在，空仓库守卫不再需要）。
- M1-1：`src/shared/{types,presets,constants,errors,text}.ts` 与单测。Azure 预设 `baseUrl` 用 `https://example.openai.azure.com/openai/v1`（规划里的「资源名称」不是合法 URL）。`resolvedBaseUrl` 不读 `process.env`（shared 也会进渲染进程）。

### 怎么验证的

- `npm run check`：25 条单测通过。

### 没做成 / 坑

- M0-8 仍等这次 CI 修复推上去之后的 `check` + `package`。
- GitHub `subscribe_github_ci` 无法校验对本仓库的访问，改用推送后看 Actions。

### 下一步

- CI 绿了勾 M0-8；接着 M1-2 设置与密钥存储。

### 提交

- `ci: 去掉非法的 job 级 hashFiles 条件`
- `feat: 落地共享层类型与文案工具`

## 会话 3（Cursor Grok 4.6，16:32）

### 目标

M1-2 设置、host.json、secrets.bin。

### 做了什么

- `writeFileAtomic` / `writeJsonAtomic`（fsync + rename，Windows 上 EPERM/EEXIST 重试）。
- `SettingsStore` 深合并（数组整段替换）、损坏文件备份、失效的 defaultTranslator 置空。
- `HostStore`：`DOCFLOW_DATA_DIR` > host.json > 回退目录。
- `SecretsStore` 注入 Cryptor；单测用明文假加密器。

### 怎么验证的

- `npm run check`：43 条单测通过。

### 下一步

- M1-3 翻译 HTTP/请求/响应/错误分类。

### 提交

- `feat: 实现设置与密钥的原子读写`

## 会话 4（Cursor Grok 4.6，16:34）

### 目标

M1-3 翻译 HTTP、URL/请求体、响应解析、错误分类。

### 做了什么

- `translate/http.ts`、`request.ts`、`response.ts`、`errors.ts`、`providers.ts`（listModels / checkModel / postChat）。
- 单测覆盖 URL 四种类型、extraBody 深合并、三种响应、状态码与正文分类、Retry-After、redact。

### 怎么验证的

- `npm run check`：57 条单测通过。

### 下一步

- M1-4 `keys.ts` 与 `pool.ts`（假时钟）。

### 提交

- `feat: 实现翻译请求构造与错误分类`

## 会话 5（Cursor Grok 4.6，16:45）

### 目标

勾选 M0-8；完成 M1-4 密钥轮换与并发池。

### 做了什么

- 确认 CI run `35705675193`（`b0cd017`）`check` + `package`（macos-15、windows-latest）全绿，勾选 M0-8。
- `KeyRing`：轮询、credential 下架 600 s（`FakeClock`）、全部下架文案。
- `ProviderPool`：FIFO 队列、429 每 10 s 最多减半一次、成功回升 100→50→62→77→96→100、`setConfigured` 不打断进行中的请求、credential 换下一把 Key。
- `TranslationPools`：按服务商缓存池，`resetKeys` 重读 secrets。

### 怎么验证的

- `npm run check`：69 条单测通过。
- CI：https://github.com/Uniseem/docflow/actions/runs/35705675193

### 没做成 / 坑

- 并发上限单测若只 `release` 一次，唤醒后的后续请求会再次挂住；`release` 后新 fetch 需立即完成。

### 下一步

- M1-5 mock 大模型服务（08.3）。

### 提交

- `feat: 实现密钥轮换与翻译并发池`

## 会话 6（Cursor Grok 4.6，16:52）

### 目标

M1-5 mock 大模型服务。

### 做了什么

- `tests/mock-provider/server.ts`：三种 API、默认译文后缀 `〔测试译文〕`、08.3 故障注入、`GET /stats`、`POST /reset`。
- `npm run mock:provider` 默认端口 38111。

### 怎么验证的

- `npm run check`：74 条单测通过。
- `npm run mock:provider` 后 `curl` `/v1/models` 与 chat 返回 `Hello〔测试译文〕`。

### 下一步

- M1-6 批处理、占位符、校验、缓存、文档翻译与假服务商。

### 提交

- `feat: 增加本地 mock 大模型服务`

## 会话 7（Cursor Grok 4.6，16:58）

### 目标

M1-6 批处理、占位符、校验、缓存、文档翻译、假服务商。

### 做了什么

- `batch.ts`：`smartSplit`（3.x 边界规则）、分批、`parseBatch`、系统/用户提示词（协议段原文照抄）。
- `protect.ts` / `validate.ts`：批次内唯一 `DOCFLOWKEEP` 标记、损坏修复、编号变化拒绝、PDF 序列校验。
- `cache.ts`：fingerprint 失效、PDF 标记序列命中条件、原子落盘。
- `translate-document.ts`：批次 → 片段阶梯 → 隔离修复、传输重试、取消、`mostly_untranslated`。
- `fake.ts` + `DOCFLOW_FAKE_PROVIDERS=1` 走内存 fetch。

### 怎么验证的

- `npm run check`：87 条单测通过（含对 mock 服务的 DROP_ME / DAMAGE / 缓存 / 取消 / REFUSE）。

### 下一步

- M1-7 日志；核对 `src/main/translate` 覆盖率 ≥ 85%。

### 提交

- `feat: 实现文档翻译阶梯与占位符保护`

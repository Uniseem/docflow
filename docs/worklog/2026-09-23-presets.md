# 2026-09-23 按服务商调研更新预设表

## 会话 1（Kimi Work，18:39 开始）

### 目标

落实 `docs/reference/providers/README.md`「对预设清单的净改动」清单：改 `src/shared/presets.ts` 与规划文档，使 4.0 预设表与 19 家服务商调研结论一致。

### 做了什么

- **`src/shared/presets.ts`**：
  - 移除 `lingyi`（零一万物）预设——平台 2026-09-03 已停止 API 服务（出处见 `lingyi.md`）。
  - 新增 `minimax` 预设：name「MiniMax」，type openai，baseUrl `https://api.minimaxi.com/v1`，keyUrl `https://platform.minimaxi.com`，group domestic（插在原 lingyi 位置）。
  - DeepSeek baseUrl 由 `https://api.deepseek.com/v1` 改为 `https://api.deepseek.com`（官方写法不带 `/v1`，`/v1` 路径仍兼容，但预设应与官方一致）。
  - 预设总数不变（仍是 20），`presets.test.ts` 的数量与唯一性断言无需改动。
- **`docs/plan/04-translation.md` 4.2**：预设表同步三处改动，界面分组清单 lingyi → minimax。
- **温度策略定论（补进 4.3）**：查代码确认请求构造全仓库只在 ProvidersPanel 的 UI 提示里出现 temperature 字样，`buildRequest` 本来就不传温度，`ModelConfig` 也没有温度字段。因此写入规划：默认不传 `temperature`（用服务商模型默认值），需要固定温度的（如 DeepSeek 官方翻译建议 1.3）由用户在 extraBody 自行配置。
- **默认并发与思考开关：不改代码**。理由：`ProviderConfig.concurrency` 默认 100 + 429 自适应已是调研中各家结论的共同兜底；预设表结构保持简单（`{id,name,type,baseUrl,keyUrl,group}`），不为它加 `defaultConcurrency`/`extraBody` 字段。各家推荐安全并发值留在调研笔记里供用户参考。
- **Prettier 全量修复**：上一轮提交的 19 份 docs 文件未过 `prettier --check`（导致 `npm run check` 退出码 1），`npx prettier --write .` 统一修复。顺带修正 `package-lock.json`。

### 怎么验证的

- `npm run check`（verify:fonts + typecheck + eslint + prettier + 测试）：通过，exit=0。

### 没做成 / 坑

- 上一轮（providers-round2）提交时未跑 `npm run check`，19 份 markdown 带着 prettier 风格问题进了 main，本轮才发现。以后每轮提交前必须跑完整 check。

### 下一步

- 翻译主流程（4.3 之后的段落翻译管线）实现或 UI 面板开发，视里程碑安排。
- 可选后续项：给 MiniMax Token Plan 加第二个预设（同 baseUrl、sk-cp Key）——README 里标为可选，等预设表结构确有需要时再加。

### 提交

- `fd78a8b feat: 按服务商调研更新预设表`

# 2026-09-25 发布 4.0.0

## 会话 1（Claude Code / Opus 5.5，Windows 机器）

### 目标

维护者决定不再做剩余任务（M5-8、M6-2、M6-3、M7-1～M7-4），直接发布 `v4.0.0`：Windows 提供安装程序（NSIS setup），macOS 用终端一行命令安装 `.pkg`。

### 做了什么

- 本地 `main` 落后 `origin/main` 4 个提交（M7-5 对齐与两轮修复），快进同步；`package.json`/`package-lock.json` 没变，不用重新安装依赖。
- `CHANGELOG.md`：Unreleased 定版为 `[4.0.0] - 2026-09-25`，加一句版本概述与比较链接。
- `docs/plan/09-milestones.md` 开头注明维护者的决定；未做的任务保持未勾选，留作 4.0.x 待办。
- `docs/plan/07-build-ci-release.md`：7.5 第 4 步注明没有打 beta 标签、改为在 `main` 上手动触发 release.yml 演练；7.7 注明体积按现状接受。
- 发布流程与产物沿用现有 `release.yml` / `electron-builder.yml`，没有改动：Windows `DocFlow-4.0.0-win-x64-setup.exe`，macOS arm64/x64 各一份 `.pkg`、`.dmg`、`.zip`，加 `SHA256SUMS.txt`；Release 说明取 `.github/release-notes.md`，里面有带版本号的 macOS 终端命令。

### 怎么验证的

- `npm run check`（本机 Windows）：通过，57 个文件、298 个单测。
- `origin/main` 最新提交 `5bb662a` 的 CI（check、windows/macOS 打包冒烟与 E2E）全绿。
- release.yml 在 4.x 上从未成功跑过（仅有 3 次早期 0 秒失败），Windows NSIS 安装包也从未在 CI 打过；所以先在 `main` 上 `workflow_dispatch` 演练（只打包、上传工件、不发布），通过后在同一提交打 `v4.0.0`。
- 第一次演练（run 36130623374，`e628152`）：Windows 成功（setup 工件压缩后 125 MB）；macOS 在 electron-builder 失败，`ENOENT: unlink '…/release/com.uniseem.docflow.pkg'`。
- 第二次演练（run 36131108905，`f7e9510`，拆分 macOS 架构后）：三个 job 全部成功，macOS 每个架构 3 个文件、Windows 1 个。
- 在 `f7e9510` 打 `v4.0.0` 并推送，release.yml（run 36131542403）四个 job 全部成功；[Release](https://github.com/Uniseem/docflow/releases/tag/v4.0.0) 为正式版并成为 Latest，8 个文件：`win-x64-setup.exe` 125.7 MB，`macos-arm64` 的 dmg/pkg/zip 各约 147 MB，`macos-x64` 的各约 150.9 MB，加 `SHA256SUMS.txt`。
- 用 README 的 macOS 一行命令里的查询（`releases/latest` 里找 `-macos-<arch>.pkg`）解析出两个架构的 v4.0.0 pkg 链接，与 Release 说明里带版本号的链接、Windows setup 链接一样都返回 HTTP 200。没有在实机上下载安装。

### 没做成 / 坑

- macOS 一次运行打 `--arm64 --x64` 两个 pkg 会失败：`app-builder-lib/out/targets/pkg.js` 把中间组件包（`<appId>.pkg`）和 `distribution.xml` 放在 `release/` 下、文件名不含架构，两个架构并行构建时一个 `unlink` 掉另一个的文件；就算碰巧不报错，`distribution.xml`（含 `hostArchitectures`）也可能被另一个架构覆盖。修复：release.yml 的矩阵拆成 macOS arm64、macOS x64 两个 job，各自一次 electron-builder；工件名与 Electron 缓存键改按 `matrix.key`。07.5 同步改写。
- 跳过的任务与验收：真实论文人工检查（M5-8）、两平台本地打包清单（M6-2）、beta 演练与下载安装验证（M6-3）、M7-1～M7-4 实测；08.6 发布前人工验收清单一项都没有做。
- 安装包体积约 156 MB，超出 130 MB 目标，按现状发布。

### 下一步

- 4.0.x：补做上面跳过的实测（尤其 M5-8 真实论文与 Windows/macOS 实机安装、升级安装）。

### 提交

- `e628152 chore: 发布 4.0.0`
- `f7e9510 ci: macOS 两个架构分开打包`（标签 `v4.0.0`）
- `docs: 记录 4.0.0 发布结果`

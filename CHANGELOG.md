# 更新日志

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- 照搬 PDFMathTranslate-next 2.9.0 / BabelDOC 0.6.4 的其余优点（ADR-0018）：
  - 翻译改为 BabelDOC 的多段 JSON 请求：同一页的段落约 200 token 或 6 段一批，跨页、跨栏的段落成对发送，带全文标题与最近小节标题作上下文；模型漏段、原样照抄、长度离谱时逐段重试，仍失败则保留原文。请求数约为原来的 1/5。
  - 术语表：翻译前自动提取全文术语并统一译名（「设置 → 高级 → 自动提取术语表」，默认开），结果可在「导出 → 术语表（CSV）」保存；可以导入自己的 CSV 术语表（source、target 两列），新建翻译时使用已勾选的术语表。
  - 段内格式：粗体、斜体、彩色的片段（如蓝色的引用链接）在译文里保留；没翻译的作者名、数字、短标签按原字形原样画。
  - 译文字体按原文的粗细、斜体、有无衬线从思源宋体、思源黑体、Noto、霞鹜文楷中挑选，也可以固定为宋体、黑体或楷体。
  - 双语对照默认改为左右并排（原文在左），可改回原文页与译文页交替，可选「译文在前」；书签迁移到双语 PDF。
  - 新建翻译可填页码范围（如 `1-3,5,8-`），只翻译这些页，可选只输出这些页。
  - 带 OCR 文字层的扫描件：打开「设置 → 高级 → 自动处理带文字层的扫描件」后，译文用黑色写在白底上盖住图片里的原文。
- 设置新增「最短翻译长度」「保留段内格式」「双语对照排法」「译文在前」「译文字体」。

### 变更

- 「翻译提示词」改为「角色提示词」：只替换 BabelDOC 提示词开头的角色说明（默认留空），格式、占位符与术语表的要求不受影响；4.0.x 的提示词模板自动清空。
- 扫描件判定：只有图片、没有文字层的 PDF 仍报「没有文字层」；有文字层的按 BabelDOC 的方法比较去掉文字前后的页面（再加「图片占页面一半以上」一条，避免字少的普通文档被误判），扫描件在设置打开前提示去打开。
- 安装包增加 BabelDOC 用的 14 个字体（约 95 MB 未压缩，SIL OFL 1.1）。
- 译文排版改用 BabelDOC 0.6.4（PDFMathTranslate 的后继项目）的做法：每段在原段落框内重排，拉丁词不拆开、标点不出现在行首，放不下时缩小字号并向下方或右侧空白扩展，全文字号统一；单行标题、图注不再向右溢出，长译文不再压到下面的内容（ADR-0017）。

### 修复

- 译文与重画的公式保留原文颜色（彩色标题、公式颜色）。
- 小字号的空格（例如 6.4 pt 的 `dummy-space`）不再被当成角标公式，译文词间不再出现空隙；各种不换行空格统一当空格处理。
- `Times-Italic`、`CMBX`（LaTeX 粗体）等字体的整段文字不再被当成公式而不翻译。
- 内联图片不再被删除；`/Rotate` 旋转过的页面，译文写在正确位置；转了 90° 的公式字符按原方向重画。

## [4.0.1] - 2026-09-26

修复译文与原文叠在一起的问题：PDF 处理改为照搬 PDFMathTranslate 1.9.11。

### 变更

- PDF 处理改为照搬 PDFMathTranslate 1.9.11：DocLayout-YOLO 版面模型（MuPDF.js 渲染页面）划分段落与保留区域，逐字符识别公式，删除页面上的全部原文文字后整页重绘，译文按原文换行排版（拉丁字符 Times、中文思源宋体）。原文不再残留在译文下面（ADR-0016）。
- 翻译改为每段单独请求，使用 PDFMathTranslate 的提示词；「设置 → 高级 → 翻译提示词」改为它的模板（`$text`、`$lang_in`、`$lang_out`），旧的默认提示词自动替换。去掉批量请求的三个参数与「译文最小缩放」。
- 中文字体由 Noto Sans SC 换成思源宋体；安装包内置版面模型（75 MB）。
- 安装包包含 AGPL-3.0 的 MuPDF.js，分发的安装包整体按 AGPL-3.0 提供（见 README「许可」）。
- Windows 窗口去掉「文件 / 编辑 / 窗口 / 帮助」菜单栏，只保留系统标题栏；菜单里的功能在设置与「关于」里都有，快捷键不变。

### 修复

- 译文与英文原文叠在一起：4.0.0 只删除按几何位置找到的段落算子，找错或找漏时原文残留。

## [4.0.0] - 2026-09-25

完全重写：Electron + React + HeroUI 3，全部 TypeScript，不再需要 MinerU、Python 或 BabelDOC。Windows 提供安装程序，macOS 用终端一行命令安装（见 README）。

### 新增

- 工程骨架：Electron 44 + React 19 + HeroUI 3 空窗口可启动，含 lint/单测/图标与 `dist:dir` 打包。
- 翻译子系统：共享类型与设置存储、请求构造与错误分类、密钥轮换（600 s 下架）和自适应并发池（100→50→62→77→96→100）。
- 本地 mock 大模型服务（OpenAI / Anthropic / Gemini 接口与 08.3 故障注入）。
- 翻译批处理、占位符保护、回复校验、缓存、文档翻译阶梯与假服务商。
- 主进程日志（electron-log，文件滚动 8 MiB）。
- PDF 测试 fixture：08.2 合成样例与两篇 CC-BY arXiv 论文。
- PDF 解析：inspect、字形/行/栏/公式/段落、表单引用、analyze worker 与调试脚本。
- PDF 写回：内容流词法分析与遍历、CJK 字体嵌入、译文排版、公式重绘、双语 PDF 与校验。
- 文档库、任务调度、PDF 流水线与 IPC：`documents:create` 可跑通 inspect→翻译→写回，产出中文 PDF 与双语 PDF。
- 渲染进程界面：文档库、新建翻译、详情预览与导出、设置（服务商/网络/高级/关于）；E2E 七个场景通过。
- 打包后窗口可挂上 React：沙箱 preload 把 zod 打进 CJS（见 ADR-0009）。
- 代理设置生效；窗口大小与位置记忆；菜单「检查更新…」给出结果；双击 PDF 冷启动也能打开新建翻译。
- 服务商调研笔记：19 家云端与本机服务商的模型、价格、限流与温度建议（`docs/reference/providers/`）。

### 变更

- 项目重建：主分支清空，旧代码移至 `old` 分支；新建标准仓库结构与 4.0 规划（Electron + React + HeroUI 3）。
- 设置了 `DOCFLOW_DATA_DIR` 时 Electron `userData` 也改到该目录下，E2E 不再改写真实用户目录里的 `host.json`。
- 退出或更换文档库不再把进行中的文档标成「已取消」，下次打开时从断点继续；取消与删除最多等待 5 秒。
- 安装包去掉从未使用的 pdf.js 资源副本（约 2.4 MB）；在分支上手动运行发布流程只打包不发布。
- E2E 与冒烟脚本默认隐藏窗口（`DOCFLOW_HIDE_WINDOW=1`），不再在屏幕上反复开关窗口。
- README 改为 4.0 的安装与使用说明；安装包不再包含 pdf.js 的可选原生模块。
- 规划文档（`docs/plan/`）与实现对齐，补齐 ADR 0010–0015；README「已知问题」补充小字号文字、图例与上标脚注。
- 服务商预设按调研更新：移除已停止服务的零一万物，新增 MiniMax；DeepSeek 地址改为官方写法（不带 `/v1`）。请求默认不传温度。

### 修复

- 修复 M5 复查发现的 48 个问题：文档库打不开导致应用无法启动、处理记录为空或重复、列表随进度跳动、筛选计数错乱、获取模型列表静默失败、导出与错误提示等。
- 写回的每一页内容流混入压缩字节（MuPDF 等阅读器报错）；间接长度的图片流被截掉一个字节。
- 两栏论文被逐行打散：分栏检测、阅读顺序、斜体句子、图内文字、环绕图的段落合并。
- Ollama、LM Studio 等本机服务无需 API Key 时无法翻译。
- 文字矩阵或坐标系带缩放的 PDF（`1 Tf` 配合缩放的 `Tm`）字距与换行算错，段落与写回位置错乱。
- 开发模式下找不到内置字体导致写回失败；「打开日志」按钮报内部错误；页数多的 PDF 分析阶段超时按 1 页计算。
- 共用字体资源的页面公式字体互相覆盖；编码失败时表单原文被删；公式颜色延续到后面的译文；共享表单的子表单被改写或送去翻译。
- 翻译服务长时间不可用时文档永久失败且反复重试；网络错误、模型列表失败显示英文或「内部错误」；Key 无效仍在重试；占位符两侧空格丢失。
- 取消或超时一份文档会让同时处理的其他文档失败；处理记录里的英文警告与内部段落编号；ZIP 导出后「打开所在文件夹」失败；关闭窗口后没有完成通知；0 字节 PDF 未被拒绝。

## [3.1.0] - 2026-09-12

旧版本线（Rust 引擎 + BabelDOC）。见 `old` 分支与 GitHub Releases。

[Unreleased]: https://github.com/Uniseem/docflow/compare/v4.0.1...HEAD
[4.0.1]: https://github.com/Uniseem/docflow/compare/v4.0.0...v4.0.1
[4.0.0]: https://github.com/Uniseem/docflow/compare/v3.1.0...v4.0.0
[3.1.0]: https://github.com/Uniseem/docflow/releases/tag/v3.1.0
